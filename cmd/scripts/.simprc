###  Simp config  ###
# Run daemon in background
simp start

_simp_history() {
    local output
    output=$(simp history "$1")
    READLINE_LINE="${READLINE_LINE}${output}"
    READLINE_POINT=${#READLINE_LINE}
}

bind -x '"\C-h": _simp_history'
bind -x '"\C-p": _simp_history --paste'
